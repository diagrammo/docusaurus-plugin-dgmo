{
  "name": "docusaurus-plugin-dgmo-fixture",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "docusaurus build",
    "start": "if command -v claim-port >/dev/null 2>&1; then claim-port 4343 || { [ $? -eq 3 ] || exit 1; exit 0; }; fi; docusaurus start --port 4343"
  },
  "dependencies": {
    "@diagrammo/dgmo": "0.82.0",
    "@docusaurus/core": "^3.10.1",
    "@docusaurus/preset-classic": "^3.10.1",
    "@mdx-js/react": "^3.0.0",
    "docusaurus-plugin-dgmo": "link:../..",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "engines": {
    "node": ">=20.6"
  }
}

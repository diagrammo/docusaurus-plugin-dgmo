---
slug: /
title: All chart types
---

# Diagrammo × Docusaurus

Placeholder. The Pages workflow overwrites this file with
`dgmo-content`'s `all-chart-types.md` (every chart type) at build time. To
preview locally, run `pnpm install && pnpm start` and paste a `dgmo` fence:

```dgmo
pie Browser share
Chrome 64
Safari 20
Firefox 6
Other 10
```
